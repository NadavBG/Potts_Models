# Models are NOT tracked in git. The fitted .npy blobs (model.npy ~47 MB; ~4.4 GB
# across all families) are too large for git/Git-LFS; they travel Mac <-> Midway
# via checksummed rsync. See scripts/sync_models.sh and docs/MODEL_SYNC.md.
#
# Do not re-add a `results/**/*.npy filter=lfs` rule here.
